import { FirstCharUpCasePipe } from './first-char-up-case.pipe';

describe('FirstCharUpCasePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCharUpCasePipe();
    expect(pipe).toBeTruthy();
  });
});
